import { cn } from '@/utils/cn';
import { backgroundColors, getRandomColor } from '@/utils/color';
import { formatCurrency } from '@/utils/formatCurrency';
import { marker, kanitBold, josefinSemiBold, poppinsRegular, josefinRegular, regalDisplay } from '@/utils/fonts';

export {
  cn,
  formatCurrency,
  getRandomColor,
  backgroundColors,
  marker,
  kanitBold,
  josefinSemiBold,
  poppinsRegular,
  josefinRegular,
  regalDisplay,
};
